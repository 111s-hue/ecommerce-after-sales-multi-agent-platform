# 基于 LangGraph 的电商售后多智能体协同平台——项目需求

## 1. 项目目标

建设一个可私有化部署的电商售后 AI 助手。系统以 LangGraph 为工作流编排核心，协同订单、物流、售后政策和退款四类智能体，完成从用户咨询、业务查询、政策检索、风险判断到人工审批和售后单创建的闭环。

项目既用于真实业务原型验证，也用于展示 AI 应用开发岗位所需的 Agent 编排、Agentic RAG、工具调用、状态持久化、人工介入、服务化和工程测试能力。

## 2. 用户角色

- 消费者：查询订单、物流和退换货规则，发起退款/退货申请。
- 售后客服：查看会话轨迹、证据引用和工具结果，处理异常咨询。
- 审批人员：对退款等敏感操作进行批准或拒绝。
- 系统管理员：维护政策知识库、模型配置、权限和审计记录。

## 3. 核心业务需求

### 3.1 多智能体协同

- Supervisor 根据用户问题、上下文和安全规则路由到订单、物流、政策或退款智能体。
- 每个专业智能体以独立子图运行，共享结构化 State。
- 节点失败时返回可解释错误，并保留可追踪的节点轨迹。
- 对缺少订单号等关键参数的请求主动追问，不虚构业务数据。

### 3.2 业务工具

- 查询本人订单详情。
- 查询本人订单物流轨迹。
- 结合订单状态、签收时间和政策判断退款资格。
- 经人工批准后创建售后工单。
- 工具输入输出使用 Pydantic 校验，敏感写操作要求审批结果和幂等键。
- 所有工具调用写入审计日志。

### 3.3 Agentic RAG

- 从 Markdown 政策文档构建知识库，并按条款切分。
- 生产环境使用 Milvus 承载 BGE-M3 稠密向量与原生 BM25 稀疏向量，通过 RRF 融合并按 `tenant_id` 过滤；本地测试使用无外部依赖的轻量检索，FAISS 仅作为离线实验后端。
- 返回条款级引用、检索分数和证据等级。
- 证据不足时明确提示，并建议转人工处理。
- 防御“忽略系统规则”“泄露提示词”“越权查询”等提示注入。

### 3.4 人工审批与断点恢复

- 退款等敏感操作在执行前通过 LangGraph `interrupt` 暂停。
- 审批人可以批准或拒绝，系统通过 `Command(resume=...)` 从检查点恢复。
- 同一线程重复请求不能重复创建售后单。
- 本地开发使用内存检查点；生产可使用 Redis Checkpointer。

### 3.5 API 与管理端

- FastAPI 提供健康检查、对话、SSE 对话流、审批、订单和审计接口。
- Vue 3 + Element Plus 管理端展示会话、审批卡片、证据引用和节点轨迹。
- Qwen/vLLM 通过 OpenAI 兼容接口接入；未配置模型服务时使用可测试的模板回复。

## 4. 非功能需求

- 安全：订单按用户隔离；敏感操作强制审批；提示注入拦截；日志不记录密钥。
- 可靠性：外部调用支持超时、重试、幂等；工具异常有降级提示。
- 可观测性：每次运行记录 thread_id、节点、耗时、工具结果和审批人。
- 可部署性：支持本地 venv 和 Docker Compose；生产组件包含 MySQL、Redis Stack、MinIO。
- 可测试性：核心图不依赖外部大模型即可测试，真实模型和向量库以适配器接入。

## 5. 社区版 V1 验收标准

1. 输入“查询 ORD-1001 的物流”可返回结构化物流状态和节点轨迹。
2. 输入“ORD-1001 想退款”会判断资格并进入 `pending_approval`，在审批前不创建售后单。
3. 批准后从原 thread_id 恢复并创建唯一售后单；拒绝后不创建。
4. 政策问题返回至少一个包含文件名和条款标题的引用。
5. 用户不能查询不属于自己的订单。
6. 提示注入请求被安全节点拦截，不调用业务写工具。
7. FastAPI 健康检查、对话、审批和 SSE 接口可运行。
8. 自动化测试覆盖正常查询、缺少参数、人工审批、幂等、越权、提示注入和 RAG。
9. Docker Compose 能描述后端、前端、MySQL、Redis Stack 和 MinIO 的完整部署拓扑。

## 6. 社区版企业基线（已纳入）

- 多租户身份域、本地账号、Argon2 密码、登录锁定、RBAC 和 OIDC 连接配置。
- 仅退款、退货退款、换货、补发、维修、补偿和申诉统一售后主单与状态历史。
- 审批记录、退货物流、仓库验收、退款尝试/事件、换补修履约子单。
- 标准退款网关端口和可重复测试的 Sandbox 适配器；微信、支付宝通过同一端口扩展。
- 文档不可变版本、审核记录、索引任务和发布记录。
- 幂等记录、Outbox、站内通知以及 Webhook/邮件投递扩展表。
- Alembic 迁移、生产启动配置校验和既有 SQLite 数据无损升级。

## 7. 后续可选增强

- 微信支付、支付宝正式商户适配器及真实电商平台写操作需要部署方提供商户资质和密钥。
- OCR、语音客服、多模态商品质检和复杂租户计费作为插件能力迭代。
- 短信、企业微信与邮件供应商适配器按社区部署环境选装。

## 8. AI 工程化增强（已纳入实现）

- Qwen/vLLM Supervisor 使用 JSON Schema 输出意图、订单号、风险等级和缺失字段；模型不可用时记录降级原因。
- 专业 Agent 运行有界 Tool Calling 循环，模型只能选择白名单工具，用户身份和订单范围由可信 State 强制覆盖。
- 支持 `TOOL_TRANSPORT=mcp`，通过 Streamable HTTP MCP Client 跨进程调用业务工具服务。
- SSE 输出真实 LangGraph 节点更新和 interrupt 事件，而不是模拟进度。
- 会话、消息和审批任务持久化；Redis Checkpointer 模式下服务重启后可继续审批线程。
- 知识库支持 Markdown 上传、即时重建、MinIO 原文件同步，以及 BGE-M3/FAISS 指纹索引持久化。
- JWT/RBAC 覆盖消费者、审批人、管理员三类角色，消费者不能伪造 user_id 查询他人订单。
- 管理端包含实时工作台、审批中心、会话历史、知识库、审计日志和评测看板。
- 内置离线评测集并输出任务成功率、意图准确率和逐用例结果。
